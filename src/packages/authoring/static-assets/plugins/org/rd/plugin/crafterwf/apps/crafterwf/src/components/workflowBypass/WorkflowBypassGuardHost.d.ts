/** Ensures a single bypass guard instance even when multiple toolbar widgets mount this host. */
export declare function WorkflowBypassGuardHost(): JSX.Element;
export default WorkflowBypassGuardHost;
